/*
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.verizon.thingspace.controllers;

import com.verizon.thingspace.ApiHelper;
import com.verizon.thingspace.Server;
import com.verizon.thingspace.exceptions.ApiException;
import com.verizon.thingspace.exceptions.ETXRespondingErrorException;
import com.verizon.thingspace.exceptions.EtxResponseErrorException;
import com.verizon.thingspace.http.request.HttpMethod;
import com.verizon.thingspace.http.response.ApiResponse;
import com.verizon.thingspace.models.ClientPersistenceResponse;
import com.verizon.thingspace.models.ClientRegistrationRequestV2;
import com.verizon.thingspace.models.ClientRegistrationResponse;
import com.verizon.thingspace.models.ConfigurationListItem;
import com.verizon.thingspace.models.ConnectionRequest;
import com.verizon.thingspace.models.ConnectionResponse;
import com.verizon.thingspace.models.ConnectionResponseV3;
import com.verizon.thingspace.models.EtxID;
import com.verizon.thingspace.models.GeoFenceConfigurationRequest;
import com.verizon.thingspace.models.GeoFenceConfigurationResponse;
import com.verizon.thingspace.models.GeoFenceConfigurationUpdateRequest;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.List;
import java.util.UUID;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class APIController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public APIController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * This endpoint fetches and returns the list of configurations defined by the Vendor. The list
     * contains the configurations' identifier, name, description, and active flag. The vendor ID is
     * provided when the configuration is created through the POST request. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @return    Returns the List of ConfigurationListItem wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<ConfigurationListItem>> getConfigurationList(
            final String vendorID) throws ApiException, IOException {
        return prepareGetConfigurationListRequest(vendorID).execute();
    }

    /**
     * This endpoint fetches and returns the list of configurations defined by the Vendor. The list
     * contains the configurations' identifier, name, description, and active flag. The vendor ID is
     * provided when the configuration is created through the POST request. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @return    Returns the List of ConfigurationListItem wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<ConfigurationListItem>>> getConfigurationListAsync(
            final String vendorID) {
        try {
            return prepareGetConfigurationListRequest(vendorID).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getConfigurationList.
     */
    private ApiCall<ApiResponse<List<ConfigurationListItem>>, ApiException> prepareGetConfigurationListRequest(
            final String vendorID) {
        return new ApiCall.Builder<ApiResponse<List<ConfigurationListItem>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v1/application/configurations/geofence/ids")
                        .headerParam(param -> param.key("VendorID")
                                .value(vendorID).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        ConfigurationListItem[].class))
                        .nullify404(false)
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Configuration not found",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too many requests",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("unexpected error",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint fetches and returns a specific configuration's details. The configuration ID
     * parameter, which was provided when the configuration was created through the POST request, is
     * need to retrieve the configuration details. Note: The user needs to authenticate with their
     * ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this
     * API.
     * @param  id  Required parameter: The configuration identifier
     * @param  vendorID  Required parameter: The vendor's identifier
     * @return    Returns the GeoFenceConfigurationResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<GeoFenceConfigurationResponse> getConfiguration(
            final String id,
            final String vendorID) throws ApiException, IOException {
        return prepareGetConfigurationRequest(id, vendorID).execute();
    }

    /**
     * This endpoint fetches and returns a specific configuration's details. The configuration ID
     * parameter, which was provided when the configuration was created through the POST request, is
     * need to retrieve the configuration details. Note: The user needs to authenticate with their
     * ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this
     * API.
     * @param  id  Required parameter: The configuration identifier
     * @param  vendorID  Required parameter: The vendor's identifier
     * @return    Returns the GeoFenceConfigurationResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<GeoFenceConfigurationResponse>> getConfigurationAsync(
            final String id,
            final String vendorID) {
        try {
            return prepareGetConfigurationRequest(id, vendorID).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getConfiguration.
     */
    private ApiCall<ApiResponse<GeoFenceConfigurationResponse>, ApiException> prepareGetConfigurationRequest(
            final String id,
            final String vendorID) {
        return new ApiCall.Builder<ApiResponse<GeoFenceConfigurationResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v1/application/configurations/geofence")
                        .queryParam(param -> param.key("id")
                                .value(id))
                        .headerParam(param -> param.key("VendorID")
                                .value(vendorID).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, GeoFenceConfigurationResponse.class))
                        .nullify404(false)
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Configuration not found",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too many requests",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("unexpected error",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint creates a new configuration in the system. The data for the new configuration
     * should be provided as JSON in the body of the POST request. The system will return with a
     * unique ID for the configuration, which is needed for any further manipulation (update or
     * delete) of the configuration. Note: The user needs to authenticate with their ThingSpace
     * credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  body  Required parameter:
     * @return    Returns the GeoFenceConfigurationResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<GeoFenceConfigurationResponse> createConfiguration(
            final String vendorID,
            final GeoFenceConfigurationRequest body) throws ApiException, IOException {
        return prepareCreateConfigurationRequest(vendorID, body).execute();
    }

    /**
     * This endpoint creates a new configuration in the system. The data for the new configuration
     * should be provided as JSON in the body of the POST request. The system will return with a
     * unique ID for the configuration, which is needed for any further manipulation (update or
     * delete) of the configuration. Note: The user needs to authenticate with their ThingSpace
     * credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  body  Required parameter:
     * @return    Returns the GeoFenceConfigurationResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<GeoFenceConfigurationResponse>> createConfigurationAsync(
            final String vendorID,
            final GeoFenceConfigurationRequest body) {
        try {
            return prepareCreateConfigurationRequest(vendorID, body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for createConfiguration.
     */
    private ApiCall<ApiResponse<GeoFenceConfigurationResponse>, ApiException> prepareCreateConfigurationRequest(
            final String vendorID,
            final GeoFenceConfigurationRequest body) {
        return new ApiCall.Builder<ApiResponse<GeoFenceConfigurationResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v1/application/configurations/geofence")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("VendorID")
                                .value(vendorID).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, GeoFenceConfigurationResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid configuration",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too many requests",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("unexpected error",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint updates an existing configuration. Similar to POST, the updated data for the
     * configuration should be provided as JSON in the body of the PUT request. The configuration ID
     * parameter, which was provided by the POST (create) operation, is required to do any updates
     * on the configuration. Note: The user needs to authenticate with their ThingSpace credentials
     * using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  id  Required parameter: The configuration identifier
     * @param  body  Required parameter:
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> updateConfiguration(
            final String vendorID,
            final String id,
            final GeoFenceConfigurationUpdateRequest body) throws ApiException, IOException {
        return prepareUpdateConfigurationRequest(vendorID, id, body).execute();
    }

    /**
     * This endpoint updates an existing configuration. Similar to POST, the updated data for the
     * configuration should be provided as JSON in the body of the PUT request. The configuration ID
     * parameter, which was provided by the POST (create) operation, is required to do any updates
     * on the configuration. Note: The user needs to authenticate with their ThingSpace credentials
     * using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  id  Required parameter: The configuration identifier
     * @param  body  Required parameter:
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> updateConfigurationAsync(
            final String vendorID,
            final String id,
            final GeoFenceConfigurationUpdateRequest body) {
        try {
            return prepareUpdateConfigurationRequest(vendorID, id, body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for updateConfiguration.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareUpdateConfigurationRequest(
            final String vendorID,
            final String id,
            final GeoFenceConfigurationUpdateRequest body) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v1/application/configurations/geofence")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("id")
                                .value(id))
                        .headerParam(param -> param.key("VendorID")
                                .value(vendorID).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.PUT))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid configuration",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Configuration not found",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too many requests",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("unexpected error",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint deletes a specific configuration from the system. It requires the configuration
     * ID parameter, which was provided by the POST (create) operation. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  id  Required parameter: The configuration identifier
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> deleteConfiguration(
            final String vendorID,
            final String id) throws ApiException, IOException {
        return prepareDeleteConfigurationRequest(vendorID, id).execute();
    }

    /**
     * This endpoint deletes a specific configuration from the system. It requires the configuration
     * ID parameter, which was provided by the POST (create) operation. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  vendorID  Required parameter: The vendor's identifier
     * @param  id  Required parameter: The configuration identifier
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> deleteConfigurationAsync(
            final String vendorID,
            final String id) {
        try {
            return prepareDeleteConfigurationRequest(vendorID, id).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for deleteConfiguration.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareDeleteConfigurationRequest(
            final String vendorID,
            final String id) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v1/application/configurations/geofence")
                        .queryParam(param -> param.key("id")
                                .value(id))
                        .headerParam(param -> param.key("VendorID")
                                .value(vendorID).isRequired(false))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.DELETE))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too many requests",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("unexpected error",
                                (reason, context) -> new EtxResponseErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the user (client) registers its device or software service to the ETX
     * system. Therefore, when a connection is initiated from the device or software service to the
     * ETX system along with the credential provided by this registration call, then the connection
     * will be authorized. - The user can register multiple devices or software services, which can
     * all be used at the same time. - There rules set in the system that limit the type and subtype
     * of the clients that are allowed to be registered under the VendorID. The rules are created
     * based ont he agreement between the Vendor and Verizon. - The user will only be able to
     * register a limited number of devices or software services under the same VendorID. This
     * registration limit is specified by the agreement between the Vendor and Verizon. Note: The
     * user needs to authenticate with their ThingSpace credentials using the Access/Bearer and
     * Session/M2M tokens in order to call this API.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ClientRegistrationResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ClientRegistrationResponse> registerETXDevice(
            final ClientRegistrationRequestV2 body,
            final UUID xTransactionId) throws ApiException, IOException {
        return prepareRegisterETXDeviceRequest(body, xTransactionId).execute();
    }

    /**
     * With this API call the user (client) registers its device or software service to the ETX
     * system. Therefore, when a connection is initiated from the device or software service to the
     * ETX system along with the credential provided by this registration call, then the connection
     * will be authorized. - The user can register multiple devices or software services, which can
     * all be used at the same time. - There rules set in the system that limit the type and subtype
     * of the clients that are allowed to be registered under the VendorID. The rules are created
     * based ont he agreement between the Vendor and Verizon. - The user will only be able to
     * register a limited number of devices or software services under the same VendorID. This
     * registration limit is specified by the agreement between the Vendor and Verizon. Note: The
     * user needs to authenticate with their ThingSpace credentials using the Access/Bearer and
     * Session/M2M tokens in order to call this API.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ClientRegistrationResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ClientRegistrationResponse>> registerETXDeviceAsync(
            final ClientRegistrationRequestV2 body,
            final UUID xTransactionId) {
        try {
            return prepareRegisterETXDeviceRequest(body, xTransactionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for registerETXDevice.
     */
    private ApiCall<ApiResponse<ClientRegistrationResponse>, ApiException> prepareRegisterETXDeviceRequest(
            final ClientRegistrationRequestV2 body,
            final UUID xTransactionId) {
        return new ApiCall.Builder<ApiResponse<ClientRegistrationResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v2/clients/registration")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ClientRegistrationResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("503",
                                 ErrorCase.setReason("Internal Server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the user (client) can: - renew the certificate of a device or software
     * service in the ETX system if the original certificate has expired. If the client's
     * certificate expired or going to expire within 30 days and new certificate will be issued. If
     * the certificate expires more than 30 days, the current certificate will be returned to the
     * client. - complete its device or software service registration to the ETX system if the
     * original registration request was not successful because of a pending certificate generation.
     * Whenever the user receives a "client registration is pending" response (HTTP 202) from POST
     * /clients/registration call. The client should initiate this PUT API call to finish the
     * registration process and get the required certificate. Note: The user needs to authenticate
     * with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to
     * call this API.
     * @param  etxDeviceID  Required parameter:
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @param  body  Optional parameter:
     * @return    Returns the ClientRegistrationResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ClientRegistrationResponse> renewETXDevice(
            final UUID etxDeviceID,
            final String etxVendorID,
            final UUID xTransactionId,
            final Object body) throws ApiException, IOException {
        return prepareRenewETXDeviceRequest(etxDeviceID, etxVendorID, xTransactionId,
                body).execute();
    }

    /**
     * With this API call the user (client) can: - renew the certificate of a device or software
     * service in the ETX system if the original certificate has expired. If the client's
     * certificate expired or going to expire within 30 days and new certificate will be issued. If
     * the certificate expires more than 30 days, the current certificate will be returned to the
     * client. - complete its device or software service registration to the ETX system if the
     * original registration request was not successful because of a pending certificate generation.
     * Whenever the user receives a "client registration is pending" response (HTTP 202) from POST
     * /clients/registration call. The client should initiate this PUT API call to finish the
     * registration process and get the required certificate. Note: The user needs to authenticate
     * with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to
     * call this API.
     * @param  etxDeviceID  Required parameter:
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @param  body  Optional parameter:
     * @return    Returns the ClientRegistrationResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ClientRegistrationResponse>> renewETXDeviceAsync(
            final UUID etxDeviceID,
            final String etxVendorID,
            final UUID xTransactionId,
            final Object body) {
        try {
            return prepareRenewETXDeviceRequest(etxDeviceID, etxVendorID, xTransactionId,
            body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for renewETXDevice.
     */
    private ApiCall<ApiResponse<ClientRegistrationResponse>, ApiException> prepareRenewETXDeviceRequest(
            final UUID etxDeviceID,
            final String etxVendorID,
            final UUID xTransactionId,
            final Object body) {
        return new ApiCall.Builder<ApiResponse<ClientRegistrationResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v2/clients/registration")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("etxDeviceID")
                                .value(etxDeviceID).isRequired(false))
                        .headerParam(param -> param.key("etxVendorID")
                                .value(etxVendorID).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.PUT))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ClientRegistrationResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("503",
                                 ErrorCase.setReason("Internal Server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the user (client) can unregister its devices and software services from
     * the ETX system. The unregistered devices and services will no longer be able to use the ETX
     * Message Exchange. Note: The user needs to authenticate with their ThingSpace credentials
     * using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  deviceIDs  Required parameter: The list of device IDs and software service IDs to be
     *         unregistered
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> unregisterETXDevice(
            final String etxVendorID,
            final List<UUID> deviceIDs,
            final UUID xTransactionId) throws ApiException, IOException {
        return prepareUnregisterETXDeviceRequest(etxVendorID, deviceIDs, xTransactionId).execute();
    }

    /**
     * With this API call the user (client) can unregister its devices and software services from
     * the ETX system. The unregistered devices and services will no longer be able to use the ETX
     * Message Exchange. Note: The user needs to authenticate with their ThingSpace credentials
     * using the Access/Bearer and Session/M2M tokens in order to call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  deviceIDs  Required parameter: The list of device IDs and software service IDs to be
     *         unregistered
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> unregisterETXDeviceAsync(
            final String etxVendorID,
            final List<UUID> deviceIDs,
            final UUID xTransactionId) {
        try {
            return prepareUnregisterETXDeviceRequest(etxVendorID, deviceIDs, xTransactionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for unregisterETXDevice.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareUnregisterETXDeviceRequest(
            final String etxVendorID,
            final List<UUID> deviceIDs,
            final UUID xTransactionId) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v2/clients/registration")
                        .queryParam(param -> param.key("DeviceIDs")
                                .value(deviceIDs))
                        .headerParam(param -> param.key("etxVendorID")
                                .value(etxVendorID).isRequired(false))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.DELETE))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("503",
                                 ErrorCase.setReason("Internal Server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the user can check the certificate of the device. At least one of the
     * DeviceID, IMEI, ICCID or IMSI is required to make the call. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  etxID  Required parameter: One of the following IDs is required- DeviceID, IMEI,
     *         ICCID, IMSI. If more than one ID is provided, the API will return the certificate for
     *         the first ID found. The IDs are evaluated in the following order: DeviceID, IMEI,
     *         ICCID, IMSI. If the first provided ID is not found, the API will return an error.
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ClientPersistenceResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ClientPersistenceResponse> getETXDeviceCertificate(
            final EtxID etxID,
            final UUID xTransactionId) throws ApiException, IOException {
        return prepareGetETXDeviceCertificateRequest(etxID, xTransactionId).execute();
    }

    /**
     * With this API call the user can check the certificate of the device. At least one of the
     * DeviceID, IMEI, ICCID or IMSI is required to make the call. Note: The user needs to
     * authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens
     * in order to call this API.
     * @param  etxID  Required parameter: One of the following IDs is required- DeviceID, IMEI,
     *         ICCID, IMSI. If more than one ID is provided, the API will return the certificate for
     *         the first ID found. The IDs are evaluated in the following order: DeviceID, IMEI,
     *         ICCID, IMSI. If the first provided ID is not found, the API will return an error.
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ClientPersistenceResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ClientPersistenceResponse>> getETXDeviceCertificateAsync(
            final EtxID etxID,
            final UUID xTransactionId) {
        try {
            return prepareGetETXDeviceCertificateRequest(etxID, xTransactionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getETXDeviceCertificate.
     */
    private ApiCall<ApiResponse<ClientPersistenceResponse>, ApiException> prepareGetETXDeviceCertificateRequest(
            final EtxID etxID,
            final UUID xTransactionId) {
        return new ApiCall.Builder<ApiResponse<ClientPersistenceResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v2/clients/registration")
                        .queryParam(param -> param.key("etxID")
                                .value(etxID))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ClientPersistenceResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Not Found",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("500",
                                 ErrorCase.setReason("Internal server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the device or software service requests the MQTT URL for the location that
     * it needs to connect. To determine the proper URL the device or software service needs to
     * provide its ID (the one that was provided in the registration request), location (GPS
     * coordinates), and whether it is on the Verizon cellular network or not. Note: The user needs
     * to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M
     * tokens in order to call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ConnectionResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ConnectionResponse> retrieveMQTTURL(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) throws ApiException, IOException {
        return prepareRetrieveMQTTURLRequest(etxVendorID, body, xTransactionId).execute();
    }

    /**
     * With this API call the device or software service requests the MQTT URL for the location that
     * it needs to connect. To determine the proper URL the device or software service needs to
     * provide its ID (the one that was provided in the registration request), location (GPS
     * coordinates), and whether it is on the Verizon cellular network or not. Note: The user needs
     * to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M
     * tokens in order to call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ConnectionResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ConnectionResponse>> retrieveMQTTURLAsync(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) {
        try {
            return prepareRetrieveMQTTURLRequest(etxVendorID, body, xTransactionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for retrieveMQTTURL.
     */
    private ApiCall<ApiResponse<ConnectionResponse>, ApiException> prepareRetrieveMQTTURLRequest(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) {
        return new ApiCall.Builder<ApiResponse<ConnectionResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v2/clients/connection")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("etxVendorID")
                                .value(etxVendorID).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ConnectionResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("503",
                                 ErrorCase.setReason("Internal server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * With this API call the device or software service requests the MQTT URL for the location that
     * it needs to connect. To determine the proper URL the device or software service needs to
     * provide its ID (the one that was provided in the registration request), location (GPS
     * coordinates), and whether it is on the Verizon cellular network or not. If there are multiple
     * MECs that serve the location of the client all options are provided in the response, and the
     * client is free to choose which MEC they want to connect. Note: The user needs to authenticate
     * with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to
     * call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ConnectionResponseV3 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ConnectionResponseV3> retrieveMQTTURLMultiMEC(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) throws ApiException, IOException {
        return prepareRetrieveMQTTURLMultiMECRequest(etxVendorID, body, xTransactionId).execute();
    }

    /**
     * With this API call the device or software service requests the MQTT URL for the location that
     * it needs to connect. To determine the proper URL the device or software service needs to
     * provide its ID (the one that was provided in the registration request), location (GPS
     * coordinates), and whether it is on the Verizon cellular network or not. If there are multiple
     * MECs that serve the location of the client all options are provided in the response, and the
     * client is free to choose which MEC they want to connect. Note: The user needs to authenticate
     * with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to
     * call this API.
     * @param  etxVendorID  Required parameter: The VendorID set during the Vendor registration
     *         call.
     * @param  body  Required parameter:
     * @param  xTransactionId  Optional parameter: Optional transaction identifier for tracing
     *         requests. If not provided, the application will generate one.
     * @return    Returns the ConnectionResponseV3 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ConnectionResponseV3>> retrieveMQTTURLMultiMECAsync(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) {
        try {
            return prepareRetrieveMQTTURLMultiMECRequest(etxVendorID, body, xTransactionId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for retrieveMQTTURLMultiMEC.
     */
    private ApiCall<ApiResponse<ConnectionResponseV3>, ApiException> prepareRetrieveMQTTURLMultiMECRequest(
            final String etxVendorID,
            final ConnectionRequest body,
            final UUID xTransactionId) {
        return new ApiCall.Builder<ApiResponse<ConnectionResponseV3>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.IMP_SERVER.value())
                        .path("/api/v3/clients/connection")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("etxVendorID")
                                .value(etxVendorID).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("X-Transaction-Id")
                                .value(xTransactionId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("thingspace_oauth")
                                        .add("sessionToken")))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ConnectionResponseV3.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden Request",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("Too Many Requests",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase("503",
                                 ErrorCase.setReason("Internal server Error",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ETXRespondingErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}